# Absolute, public URL of the deployed site. Used for canonical tags, Open
# Graph images, robots.txt and sitemap.xml. No trailing slash.
# Falls back to https://hurera.dev when unset.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
